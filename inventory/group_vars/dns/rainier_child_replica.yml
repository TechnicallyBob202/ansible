---
# POST-PROMOTION DNS for rainier.alpine.lab Child Domain Replica (QUARTZ-DC)
# Points to domain PDC first, then itself, then parent domain DCs

windows_dns_servers:
  - 192.168.35.34       # MARBLE-DC (rainier.alpine.lab child PDC)
  - 127.0.0.1           # Loopback (self)
  - 192.168.35.30       # GRANITE-DC (alpine.lab parent PDC)
  - 192.168.35.31       # BASALT-DC (alpine.lab parent replica)

windows_dns_suffix: rainier.alpine.lab
