---
# POST-PROMOTION DNS for arbor.lab Forest Root PDC (KOA-DC)
# Points to itself (127.0.0.1) and the other forest root (GRANITE-DC)

windows_dns_servers:
  - 127.0.0.1           # Loopback (self)
  - 192.168.35.30       # GRANITE-DC (alpine.lab forest root PDC)

windows_dns_suffix: arbor.lab
