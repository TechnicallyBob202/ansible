---
# Proxmox connection defaults
proxmox_user: root@pam
proxmox_password: "{{ lookup('env', 'proxmox-root-password') }}"
proxmox_validate_certs: no
snapshot_name: "post-deploy-{{ lookup('pipe','date -u +%Y%m%dT%H%M%SZ') }}"
proxmox_snapshot_vmstate: false

# Remove the hardcoded pool - will be derived from tags
proxmox_network_bridge: vmbr0
proxmox_network_vlan: 35

# Timing defaults
proxmox_clone_timeout: 600
proxmox_boot_wait_seconds: 60
proxmox_stop_timeout: 300

# Tag management (lowercase for VM tags, uppercase for pools)
proxmox_tags:
  - name: adfr
    color: "#2ecc71"  # green
    pool: ADFR
  - name: dsp
    color: "#3498db"  # blue
    pool: DSP
  - name: ad
    color: "#e74c3c"  # red
    pool: AD